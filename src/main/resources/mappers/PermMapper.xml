<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hbnu.gradesign.dao.PermMapper">
    <resultMap id="BaseResultMap" type="com.hbnu.gradesign.domain.Permission">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="permission" jdbcType="VARCHAR" property="permission"/>
    </resultMap>
    <select id="getPermsByUserId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
		SELECT
            *
        FROM
            permission
        WHERE
            id IN ( SELECT permission_id FROM role_permission WHERE role_id IN ( SELECT role_id FROM user_role WHERE user_id = #{userId} ) );
	</select>
</mapper>